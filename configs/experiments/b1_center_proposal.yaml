_base_: b1_teacher_value_no_scale.yaml

experiment:
  name: b1_center_proposal
  description: >-
    保持 B1 的 GT 尺寸、匹配、q 和双层蒸馏，用匹配 proposal 的中心绘制 Gaussian。

region:
  scaler:
    type: proposal_center_only
    enabled: true

runtime:
  output_dir: outputs/b1_center_proposal
