# 文档索引

根目录只保存当前仍然有效、可以直接指导实验的文档。

## 当前文档

- `实验计划和配置.md`：B0、B1、B1T、B2 的唯一实验定义与公共口径。
- `../labeldistill/docs/训练命令.md`：精简训练命令。

## 历史文档

旧诊断、旧统计和方案演进记录位于 `archive/`。这些材料用于追溯，不得作为当前教师、数据或实验配置的依据。

发生冲突时，真相优先级为：

```text
当前 YAML 配置与测试 > 实验计划和配置.md > archive 历史材料
```
