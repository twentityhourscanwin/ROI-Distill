# ============================================================
# ROI_LABEL_DISTILL Requirements
# 适配镜像: ppu-training:1.4.0-pytorch2.4mmcv2.1.0mmdet3d1.4.0-ppu-py310-cu123-ubuntu22.04
# 环境: Python 3.10 | CUDA 12.3 | torch 2.4.0 | pip 22.0.4
#
# 原则:
#   1. 镜像已自带的包一律不重装, 保留镜像原生版本(CUDA/算子均已编译适配)
#   2. 仅安装镜像中缺失的包, 且版本均已校验不会顶掉镜像原生依赖
#
# 安装方式(只装下面"需要安装"区块):
#   pip install -r requirements.txt
# ============================================================


# ============================================================
# 【需要安装】镜像缺失, 需补装 (版本已校验, 不会升级镜像原生包)
# ============================================================

# --- 训练框架 (镜像未自带) ---
# pytorch-lightning 升级到 2.4.x 以兼容 torch 2.4 (原 2.1.0 与 torch 2.4 不兼容)
pytorch-lightning==2.4.0
torchmetrics==1.8.2
lightning-utilities==0.15.2

# --- 以下两项 PPU 私有源无 cu123/py310 兼容构建, 且代码未 import, 不安装 ---
# mmsegmentation==1.2.2   # PPU 源仅有定制包 1.2.2+v0.1.0.ppu2.1.0.ce, 无兼容二进制
# open3d==0.19.0          # 同上; 代码中无 import open3d, 如确需再单独处理


# ============================================================
# 【镜像已自带, 请勿重装】以下为镜像原生版本, 仅作记录/对照
# 重装(尤其降级或换 cuda tag)会破坏 CUDA/算子兼容性, 切勿放开注释
# ============================================================

# --- PyTorch 核心 (镜像自带, cu123) ---
# torch==2.4.0
# torchvision==0.19.0a0
# torchaudio==2.4.0a0
# flash-attn==2.5.6

# --- OpenMMLab 核心 (镜像自带) ---
# mmcv==2.1.0
# mmdet==3.3.0
# mmengine==0.10.4
# mmdet3d==1.4.0

# --- 数据处理 (镜像自带) ---
# numpy==1.23.5
# scipy==1.9.3
# pandas==2.0.2
# numba==0.60.0+ppu1.4.2.ce   # 厂商定制版, 切勿用官方版覆盖
# llvmlite==0.43.0
# scikit-image==0.24.0
# scikit-learn==1.1.3

# --- 图像处理 (镜像自带) ---
# opencv-python==4.10.0.84
# pillow==10.4.0

# --- 数据集工具 (镜像自带) ---
# nuscenes-devkit==1.1.11
# lyft-dataset-sdk==0.0.8
# pyquaternion==0.9.9
# pycocotools==2.0.8

# --- 可视化与日志 (镜像自带) ---
# tensorboard==2.18.0
# tensorboardX==2.6
# matplotlib==3.5.3

# --- 3D 处理 (镜像自带) ---
# trimesh==4.4.7
# plyfile==1.1
# shapely==1.8.5.post1

# --- 构建工具 (镜像自带) ---
# ninja==1.10.0
# pybind11==2.13.6
# setuptools==69.5.1
# wheel==0.40.0

# --- 其他 (镜像自带) ---
# PyYAML==6.0.2
# tqdm==4.67.1
# addict==2.4.0
# fire==0.7.0
# networkx==3.4.2
# protobuf==3.20.2
