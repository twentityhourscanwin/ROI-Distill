# 训练命令汇总



```
python labeldistill/exps/nuscenes/ablation_param/param_J1_wl02_wh05.py --gpus 8 -b 4
python labeldistill/exps/nuscenes/ablation_param/param_mu_010.py --gpus 8 -b 4
python labeldistill/exps/nuscenes/ablation_param/param_mu_020.py --gpus 8 -b 4
python labeldistill/exps/nuscenes/ablation_param/param_P3_lambda_055.py --gpus 8 -b 4
python labeldistill/exps/nuscenes/ablation_param/param_P3_lambda_065.py --gpus 8 -b 4
```



 python labeldistill/exps/nuscenes/ablation_param/param_J2_wl03_wh06.py --gpus 4 -b 16

---

## 教师网络 CenterPoint 验证集评测

在 nuScenes val 上评估 `train_teacher_centerpoint.py` 训练出的教师权重（输出 mAP / NDS 到 `./outputs/train_teacher_centerpoint/`）：

```
# 单卡
python labeldistill/exps/nuscenes/labeldistill/train_teacher_centerpoint.py \
  -e \
  --ckpt_path ./outputs/train_teacher_centerpoint/checkpoints/step_step=50200.ckpt \
  --gpus 1 -b 4

# 多卡
python labeldistill/exps/nuscenes/labeldistill/train_teacher_centerpoint.py \
  -e \
  --ckpt_path ./outputs/train_teacher_centerpoint/checkpoints/step_step=50200.ckpt \
  --gpus 4 -b 16
```

说明：
- `-e` / `--evaluate`：走 `trainer.test()`，在验证集上跑官方 `DetNuscEvaluator`
- `--ckpt_path`：指向训练产出的 `.ckpt`（无需转 `.pth`，加载时会自动提取 `model.centerpoint.*` 权重）
- 若担心混合精度数值差异，可加 `--precision 32`

---

## 论文可视化脚本

用于从 nuScenes 中自动筛选候选样本、批量出图，再人工挑选可放入论文的图例。均在项目根目录执行。

### 1. GT 与教师 BEV 响应不匹配

**脚本：** `tools/visualize_roi_distill_evidence.py`

**作用：** 说明 GT-guided 蒸馏的局限——某帧存在 GT 目标，但教师 CenterPoint 的 BEV heatmap 在该 GT 区域内几乎没有激活。若仍按 GT 做特征蒸馏，会把不可靠监督强加给学生。

**每个候选样本输出 3 张图：**

| 文件名后缀 | 内容 |
|---|---|
| `*_01_gt_bev.png` | LiDAR 俯视图 + GT 框，高亮选中目标 |
| `*_02_teacher_heatmap.png` | 教师网络 BEV heatmap |
| `*_03_gt_heatmap_overlay.png` | GT 框叠加在 heatmap 上（论文主看这张） |

**依赖：** 需加载实验配置与教师模型，默认使用 `param_P3_lambda_055` 的 `LabelDistillModel`，需 GPU 与 `./ckpts/centerpoint_vox01_128x128_20e_10sweeps.pth`。

```
# 扫描 val 前 200 帧，最多保存 20 个候选
python tools/visualize_roi_distill_evidence.py \
  --split val \
  --max-samples 200 \
  --max-figures 20 \
  --out-dir outputs/vis_gt_heatmap

# 指定某一帧（dataset index）
python tools/visualize_roi_distill_evidence.py \
  --indices 0,2,14 \
  --out-dir outputs/vis_gt_heatmap

# 同时保存 PDF
python tools/visualize_roi_distill_evidence.py \
  --split val --max-samples 200 --max-figures 20 \
  --out-dir outputs/vis_gt_heatmap --save-pdf
```

**常用参数：**

| 参数 | 默认 | 说明 |
|---|---|---|
| `--split` | `val` | 扫描 train 或 val |
| `--max-samples` | 200 | 最多扫描多少帧 |
| `--max-figures` | 20 | 最多保存多少个候选样本 |
| `--start-idx` | 0 | 从哪个 dataset index 开始 |
| `--indices` | 空 | 逗号分隔，指定 index 时忽略 start/max-samples 范围逻辑 |
| `--activation-thresh` | 0.15 | GT 内 teacher heatmap 最大值低于此值视为 mismatch |
| `--device` | `cuda` | 模型前向设备 |
| `--exp` | `param_P3_lambda_055:LabelDistillModel` | 实验类，格式 `module:ClassName` |

**输出：** `--out-dir` 下 PNG（及可选 PDF），以及 `summary.json`（含 `idx`、`token`、选中 GT 类别、`activation_max/mean`）。

---

### 2. 运动目标 LiDAR 点云超出静态 GT 框

**脚本：** `tools/visualize_lidar_motion_boundary.py`

**作用：** 说明 LiDAR 时序采样导致运动目标点云沿速度方向拉伸，实际分布可能超出静态 GT 框——支撑论文中 Adaptive GT Scaling 的动机。不加载检测模型，只读 nuScenes GT 与多 sweep LiDAR，速度较快。

**每个候选目标输出 3 张图：**

| 文件名后缀 | 内容 |
|---|---|
| `*_01_scene_bev.png` | 整帧 BEV，高亮选中运动 GT + 速度箭头 |
| `*_02_object_crop.png` | 目标局部：蓝点=框内，橙点=框外近邻点 |
| `*_03_gt_local_boundary.png` | GT 局部坐标系，最直观看越界（论文主看这张） |

**筛选条件（可改）：** 速度 ≥ `--speed-thresh`；目标附近一定数量 LiDAR 点；其中足够比例落在原始 GT 框外，且沿速度方向越界。

```
# 扫描 val，最多 20 个运动越界候选
python tools/visualize_lidar_motion_boundary.py \
  --split val \
  --max-samples 300 \
  --max-candidates 20 \
  --out-dir outputs/vis_lidar_motion_boundary

# 放宽阈值，更容易筛到案例
python tools/visualize_lidar_motion_boundary.py \
  --split val --max-samples 300 --max-candidates 20 \
  --speed-thresh 0.8 \
  --outside-ratio-thresh 0.2 \
  --motion-outside-ratio-thresh 0.08 \
  --out-dir outputs/vis_lidar_motion_boundary

# 只看车辆类、指定帧
python tools/visualize_lidar_motion_boundary.py \
  --indices 1,2 \
  --classes car,truck,bus \
  --out-dir outputs/vis_lidar_motion_boundary
```

**常用参数：**

| 参数 | 默认 | 说明 |
|---|---|---|
| `--data-root` | `data/nuScenes` | 数据集根目录 |
| `--max-candidates` | 20 | 最多保存多少个目标候选 |
| `--classes` | 车辆+两轮等 | 逗号分隔，参与筛选的类别 |
| `--speed-thresh` | 1.0 | 最低速度 (m/s) |
| `--outside-ratio-thresh` | 0.25 | 近邻点中框外比例下限 |
| `--motion-outside-ratio-thresh` | 0.12 | 沿速度方向框外比例下限 |
| `--min-near-points` | 20 | 目标附近最少 LiDAR 点数 |
| `--expand-longitudinal` | 3.0 | 纵向搜索范围 (m) |
| `--expand-lateral` | 1.5 | 横向搜索范围 (m) |
| `--z-margin` | 0.4 | GT 高度方向容差 (m) |

**输出：** `--out-dir` 下每个候选一组 3 张图，以及 `summary.json`（含 `speed`、`outside_ratio`、`motion_outside_ratio` 等）。

**人工 review 建议：** 优先看 `*_03_gt_heatmap_overlay.png`（脚本 1）和 `*_03_gt_local_boundary.png`（脚本 2）；`summary.json` 可按 `activation_max` 或 `outside_ratio` 排序挑最典型案例。

---

